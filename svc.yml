apiVersion: v1
kind: Service
metadata:
  name: sercon
  namespace: dev-group
spec:
  selector:
    connection: attachinng-service
  type: LoadBalancer   # ✅ Corrected casing
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
